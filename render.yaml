# ===== render.yaml =====
services:
  - type: worker
    name: luckybet-bot
    runtime: python311
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: DISCORD_TOKEN
        scope: runtime
